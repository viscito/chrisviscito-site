{
  "name": "@crossfade/sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Crossfade sync worker — drives the R2 kept-in-sync reconciliation on a schedule, reading each imported playlist's source via the token-service brokered reads.",
  "engines": { "node": ">=20" },
  "scripts": {
    "worker": "tsx src/worker.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
